{
  "name": "eth-gas-subgraph",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ sistemico/eth-gas",
    "create-local": "graph create --node http://localhost:8020/ sistemico/eth-gas",
    "remove-local": "graph remove --node http://localhost:8020/ sistemico/eth-gas",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sistemico/eth-gas"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.18.0",
    "@graphprotocol/graph-ts": "0.18.1"
  }
}
